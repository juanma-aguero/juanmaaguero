
                        <div class="btn-group">
                            {%- for action in record_actions %}

                            <a class="btn btn-link  btn-sm" href="{{ "{{ path('"~ route_name_prefix ~"_"~ action ~"', {id: "~ entity|lower ~".id}) }}" }}">
                                <i class="fa fa-{{ action == 'show' ? 'file-o' : action }}"></i> {{ "{{ '"~ action ~"'|trans({}, '"~ entity ~"') }}" }}
                            </a>
                            {%- endfor %}

                        </div>
